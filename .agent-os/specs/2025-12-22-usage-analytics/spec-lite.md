# Spec Summary (Lite)

Implement usage analytics to track which items are used most frequently across projects. The system aggregates item usage data, calculates total quantities used per item, identifies projects using each item, and displays this information in a dashboard view to help users optimize inventory levels and make informed purchasing decisions.

